export interface progressBarItemIn {
  name: string;
  expertise: string | number;
  description?: string;
}

export interface experienceIn {
  name: string;
  bgUrl: string;
  description?: string;
  season?: string;
  iconUrl?: string;
}
